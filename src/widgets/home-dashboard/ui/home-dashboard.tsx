"use client";

export const HomeDashboard = () => {

  return (
    <section className="w-full max-w-3xl rounded-xl border border-black/10 p-6">
      <h1 className="text-2xl font-semibold">ArloCars Admin</h1>
      <p className="mt-2 text-sm text-zinc-600">FSD + Next.js + Jotai + React Query + Prisma</p>
    </section>
  );
};
